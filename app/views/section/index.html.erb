<h2>Listening Test</h2>

<% if @participant.blank? %>
You are not a member of any listening tests
<% else %>
  <% if @sections.blank? %>
    <p>No current tasks </p>
  <% else %>
    <p>Current sections: </p>
    <ul id="sections">
      <% @sections.each do |t|%>
        <li><%= link_to "Section #{t.id}", t %> <%=t.tasks_answered_count%>/<%=t.tasks_count%></li>
      <%end%>
    </ul>
  <%end%>
<%end%>
